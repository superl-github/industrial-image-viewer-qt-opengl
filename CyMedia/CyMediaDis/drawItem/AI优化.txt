我现在的需求是要添加一个多边形item，但是当前的“ItemDrawTool”不能实现多边形的绘制了
请你帮我分析或者完成：
1、添加一个多边形item 
2、优化ItemDrawTool类，实现多边形的绘制，之前的创建移动过逻辑耦合性太强，我的想法是提供一个接口来接收鼠标移动信息，并且判断是否绘制完毕，
3、重构之后，之前的BaseItem可能有一些接口不需要/不合理了，比如接收两个点的接口，你可以重构这个BaseItem
4、重构之后，需要考虑两个外部功能，“保存item的信息”，“根据保存的信息添加item”
注意：
1、ItemDrawTool里面只能通过m_view->viewport()->installEventFilter从而在"eventFilter"里面处理鼠标事件，这是QGraphicsView的特性，只能这样处理
2、使用“qobject_cast<CyDMediaDisScen*>(m_manager->scene())->itemAtWithoutBack”是因为scen里面固定有一个显示背景图像的item，判断点击的时候需要去掉这个item
3、主要基于ItemDrawTool修改，其他item(包含baseItem)都可以为了新的移动/创建逻辑修改
4、我需要修改后的修改说明和所有源文件